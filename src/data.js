export const example = () => {
    return 'example';
  };
  // export const filtro = () => {
//   };

// personagens.sort(function (a, b) {
	
// 	return (a.name > b.name) ? 1 : ((b.name > a.name) ? -1 : 0);
 
// });


